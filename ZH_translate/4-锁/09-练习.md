# 练习

1. 去掉 acquire 中的 xchg。解释运行 xv6 时会发生什么？

2. 把 iderw 中的 acquire 移到 sleep 之前。有竞态吗？为什么在启动 xv6 并运行 stressfs 时没有观察到？用一个空循环加大临界区；现在你看到了什么？解释。

3. 做布置的作业题。

4. 在缓冲区的 flags 中设置一位不是原子操作：处理器在寄存器中复制 flags，编辑寄存器，然后写回。因此两个进程不能同时写入 flags 是很重要的。xv6 只在持有 buflock 时编辑 B_BUSY 位，但在不持有任何锁的情况下编辑 B_VALID 和 B_WRITE 标志。为什么这是安全的？