# 练习

1. 由于扇区粒度，文中对 readseg 的调用等价于 readseg((uchar*)0x100000, 0xb500, 0x1000)。在实践中，这种不精确的行为证明不是问题。为什么不精确的 readsect 不会导致问题？

2. 关于 BIOS 更长久的问题 + 安全问题。

3. 假设你想让 bootmain() 把内核加载到 0x200000 而不是 0x100000，并通过修改 bootmain() 给每个 ELF 段的 va 加上 0x100000 来实现。有什么地方会出错？是什么？

4. 引导加载器把 ELF 头部复制到任意的内存位置 0x10000 似乎有潜在危险。为什么它不调用 malloc 获得所需内存？