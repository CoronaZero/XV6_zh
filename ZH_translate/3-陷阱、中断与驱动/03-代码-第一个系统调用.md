# 代码：第一个系统调用

> 来源：book-rev7.pdf，第 33–44 页（中文翻译）

第 1 章以 initcode.S 调用一个系统调用结束。让我们再看一次 (7713)。进程把 exec 调用的参数压入进程的栈，并把系统调用号放入 %eax。系统调用号与 syscalls 数组（一个函数指针表，3350）中的条目对应。我们需要安排：int 指令将处理器从用户模式切换到内核模式；内核调用正确的内核函数（即 sys_exec）；内核能够检索 sys_exec 的参数。接下来的几个小节描述 xv6 如何为系统调用安排这一切，然后我们会发现，我们可以为中断和异常重用相同的代码。