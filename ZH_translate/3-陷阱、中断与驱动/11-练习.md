# 练习

> 来源：book-rev7.pdf，第 33–44 页（中文翻译）

1. 在 syscall() 的第一条指令处设置断点，以捕获第一个系统调用（例如 br syscall）。此时栈上有什么值？解释在该断点处 x/37x $esp 的输出，并标记每个值是什么（例如 trap 的保存的 %ebp、trapframe.eip、暂存空间等）。

2. 添加一个新系统调用。

3. 添加一个网络驱动。