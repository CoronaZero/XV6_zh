# 练习

> 来源：book-rev7.pdf，第 25–32 页（中文翻译）

1. 看看真实操作系统如何确定内存大小。

2. 如果 xv6 没有使用大页，entrypgdir 的正确声明应该是什么？

3. Unix 的 exec 实现传统上包含对 shell 脚本的特殊处理。如果要执行的文件以 #! 开头，则第一行被认为是运行该文件的解释程序。例如，如果调用 exec 运行 myprog arg1，而 myprog 的第一行是 #!/interp，则 exec 以命令行 /interp myprog arg1 运行 /interp。在 xv6 中实现对这一惯例的支持。